# Agent 产品经理学习项目维护规则

本文件适用于整个仓库。后续教学、练习审核和文档维护必须遵守以下规则。

## 指定学习资料

- 将 `https://bojieli.github.io/ai-agent-book/book/chapter1/#agent` 作为课程的指定资料之一。
- 教学前读取 `docs/05_AI_AGENT_BOOK_CH1_MAP.md`，按其中映射将网页概念联系到本项目。
- 只做摘要、解释、练习和项目映射，不大段复制原网页；需要原文时引导学习者访问来源。
- 对可能随时间变化的模型、框架或产品信息重新核验，不把网页中的时点描述当作永久事实。

## 每次教学开始前

1. 阅读 `learning/LEARNING_RECORD.md`，确认已经通过和尚未通过的单元。
2. 阅读 `learning/LEARNING_PROCESS.md`，根据已有问题调整讲解与练习。
3. 明确本次学习单元 ID、目标、练习和审核标准。
4. 不把“看过”“听懂了”视为完成，必须要求可检查的学习证据。

## 审核规则

当学习者表示某个单元已经完成时，执行一次审核。审核至少包含：

1. 用自己的话解释关键概念。
2. 对一个新场景作产品判断并说明理由。
3. 指向项目中的代码、配置、Trace 或评测证据。
4. 完成一个动手任务或错误诊断。
5. 说明边界、风险或失败处理。

使用 `learning/LEARNING_RECORD.md` 中的 10 分量表评分。总分至少 8 分，且“证据使用”
与“边界和安全”均不得为 0，才能判定通过。

## 文档更新顺序

- 审核通过：
  1. 更新 `learning/LEARNING_RECORD.md` 中对应单元的状态、证据、分数、日期和审核结论。
  2. 在 `learning/LEARNING_PROCESS.md` 追加本次过程复盘。
  3. 更新两个文档顶部的最后更新时间。
- 审核未通过：
  1. 不得把学习记录标记为通过。
  2. 在 `learning/LEARNING_PROCESS.md` 记录本次暴露的问题、证据和下一次纠正练习。
- 不得虚构学习证据、问题、分数或完成日期。
- 学习者尚未完成任何单元时，保持初始状态，不因项目搭建完成而代替学习通过。

## 版本控制

- 每次学习审核产生文档更新后，先检查差异，再提交到 Git。
- 提交信息使用 `learning: complete Uxx ...` 或 `learning: review Uxx ...`。
- 在凭据可用且远端没有冲突时推送到现有 `origin/main`。
- `.env`、Token、运行日志和项目内工具二进制不得提交。
